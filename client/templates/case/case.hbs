<div class="content">
  <div class="case-preview">
    <img src="{{brand}}" />
    <h4>{{title}}</h4>
    {{#if website}}
      <a href="{{website}}">{{website}}</a>
    {{/if}}
    <p class="description">{{{description}}}</p>
  </div>
</div>