<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:investees, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:investees, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :investee, url(:investees, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'investees/form', :locals => { :f => f } %>
 <% end %>
</div>
